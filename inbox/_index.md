# 📥 Inbox — 待分类

把新 skill 目录直接丢进这里，然后告诉 AI"帮我分类 inbox 里的新 skill"。AI 会自动扫描此目录，读取 `_index.md` 分类体系，分析并归类。

## 使用流程

1. 把新 skill 目录（含 SKILL.md + README.md）放入此目录
2. 告诉 AI："帮我分类 inbox 里的新 skill" 或 "我放了个新的 skill 在 inbox，帮我归类"
3. AI 自动扫描 → 分析 → 建议归类 → 你确认后移动 → 更新索引

不需要手动登记任何东西。
